# Models Directory

This directory stores trained expert and router models.

Pretrained weights are not included in this repository due to size limits.

After training, models will be saved under:

models/output_<model>_seed<seed>/<expert_name>/

Example:

models/output_codebert-base_seed0/CWE-189/
